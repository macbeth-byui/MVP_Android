<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="t.macbeth.mvp_android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MVP_Android">
        <activity android:name=".MVP3.MainActivity3"></activity>
        <activity android:name=".MVP3.MainActivity2" />
        <activity android:name=".MVP1.MainActivity3" />
        <activity android:name=".MVP1.MainActivity2" />
        <activity android:name=".MVP3.MainActivity" />
        <activity android:name=".MVP1.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>